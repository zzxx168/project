@model IEnumerable<project.Models.OrderTable>

@{
    ViewBag.Title = "orderL";
    Layout = "~/Views/Shared/_LayoutOld.cshtml";
}

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

<div class="menu-box">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="heading-title text-center">
                    <br />
                    <br />
                    <br />
                    <h2>訂單查詢</h2>
                </div>
            </div>
        </div>
        <div class="row special-list">
            <table  style="font-size:larger; color: black;" class="table">
                <tr>
                    <th></th>
                    <th></th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PriceTotal)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderDate)
                    </th>
                    <th>明細查詢</th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td></td>
                        <td></td>
                        <td>
                            @Html.DisplayFor(modelItem => item.OrderID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PriceTotal)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.OrderDate)
                        </td>
                        <td>
                            @Html.ActionLink("訂單明細", "orderQ", new { id = item.OrderID }) |
                        </td>
                        @{
                        }

                    </tr>
                }
            </table>
        </div>
    </div>
</div>

